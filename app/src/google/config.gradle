ext {
    // AdMob配置
    admob = [applicationId: "ca-app-pub-7869168547621070~8286549708", // 测试应用ID，请替换为实际的AdMob应用ID
             adUnitIds    : [banner      : "",        // 横幅广告测试ID
                             interstitial: "ca-app-pub-7869168547621070/1536024666", // 插页广告测试ID
                             splash      : "ca-app-pub-7869168547621070/5848088629",       // 开屏广告测试ID
                             native      : "ca-app-pub-7869168547621070/6686527571",        // 原生广告测试ID
                             full_native : "ca-app-pub-7869168547621070/5656516935",        // 全屏原生广告测试ID
                             rewarded    : ""         // 激励广告测试ID
             ]]

    // Google Ad Manager 配置（按业务替换为正式 ID）
    gam = [adUnitIds: [banner      : "",
                       interstitial: "/23312177113/GoStep_IV",
                       splash      : "/23312177113/GoStep_SP",
                       native      : "",
                       full_native : "",
                       rewarded    : "/23312177113/GoStep_RV"
    ]]

    // Pangle配置
    pangle = [applicationId: "8822399", // Pangle测试应用ID
              adUnitIds    : [splash      : "890147899",        // 开屏广告测试ID（竖屏）
                              banner      : "983106645",        // 横幅广告测试ID（320x50）
                              interstitial: "983106659",       // 插页广告测试ID（竖屏）
                              native      : "983106685",        // 原生广告测试ID
                              full_native : "983106740",        // 全屏原生广告测试ID
                              rewarded    : "983106763"         // 激励视频测试ID（竖屏）
              ]]

    // TopOn配置
    topon = [applicationId: "h1hb9ndat48oth",           // TopOn 应用 ID（需替换为实际值）
             appKey       : "aebf13daa666d3b378b53f637de267e5f",          // TopOn 应用密钥（需替换为实际值）
             adUnitIds    : [interstitial: "n1hb9ndav6r4k7",  // 插页广告位 ID（需替换为实际值）
                             rewarded    : "n1hb9ndaup5fj0",  // 激励广告位 ID
                             native      : "n1hb9ndavvpaqt",  // 原生广告位 ID（需替换为实际值）
                             splash      : "n1hb9ndb0efh5b",  // 开屏广告位 ID（需替换为实际值）
                             full_native : "n1hb9ndb0si73f",  // 全屏原生广告位 ID（需替换为实际值）
                             banner      : "n1hb9ndb1ml7fs"]  // 横幅广告位 ID（需替换为实际值）
    ]

    // MAX (AppLovin) 配置
    max = [sdkKey   : "",  // Demo SDK Key（测试用）
           adUnitIds: [splash      : "",      // 开屏广告位 ID
                       banner      : "",      // 横幅广告位 ID
                       interstitial: "",// 插页广告位 ID
                       native      : "",      // 原生广告位 ID
                       fullNative  : "", // 全屏原生广告位 ID
                       rewarded    : ""]    // 激励广告位 ID
    ]

    // 应用版本配置
    app = [applicationId: "com.stepwise.pedometer.gostep",
           compileSdk   : 35,
           minSdk       : 26,
           targetSdk    : 36,
           versionCode  : 4,
           versionName  : "1.0.3"]

    // 统计归因配置
    analytics = [adjustAppToken: "hopjs50gmuww",           // Adjust App Token
                 thinkingDataAppId: "871c0ebde6be4329b9b74acb3f08ba36",   // 数数 SDK APP ID
                 thinkingDataServerUrl: "https://ds.devs343.com",     // 数数上报域名
                 defaultUserChannel: "natural"]                           // 默认用户渠道，internal默认paid
}
