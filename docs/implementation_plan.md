# Android Compose 计步器 + 喝水应用实现方案

> 状态说明：`[ ]` 未开始，`[x]` 已完成。本文档基于已读取的 Figma 文件 `dVsTL6ggoDPXVPcEKg856G` 中 10 个节点生成：`95:986`、`2:724`、`2:1384`、`2:1137`、`2:1250`、`92:364`、`92:479`、`92:573`、`92:677`、`92:720`。

## 目标范围

- [x] 从 `/Users/apple/AndroidStudioProjects/LCB_Template` 复制项目到当前空目录 `/Users/apple/StudioProjects/lcb_countsteps`。已复制模板工程，排除 `.git`、`.gradle` 和构建产物，保留当前方案文档。
- [x] 只在 `app` 模块实现业务代码。业务代码新增在 app 模块；后续因实机发现模板广告/指标链路导致内存异常，已在 app 模块断开 `bill`、`core`、`metrics` 运行时依赖，不改动这些模板模块源码。
- [x] 使用 Jetpack Compose 复刻 Figma UI，不使用 XML 页面。已开启 Compose 构建并准备将 `MainActivity` 切换到 `setContent` 入口。
- [x] 实现真实计步功能：优先读取 Android 设备步数传感器，不使用固定 mock 数据。已接入 `SensorManager` 的 `TYPE_STEP_COUNTER`，并降级支持 `TYPE_STEP_DETECTOR`。
- [x] 实现真实喝水记录：点击加水后持久化当天饮水记录，不使用固定 mock 数据。已通过 DataStore + JSON 保存每条饮水记录。
- [x] 首页同时承载计步主功能和 Hydrate 工具入口；喝水页面作为二级页面进入。首页 Tools 卡跳转到 Hydrate 页面。

## Figma 读取结论

- [x] Logo 节点 `95:986`：512x512，主背景 `#10CEAC`，白色脚步/运动图形，双圆环描边。已替换 adaptive launcher background/foreground vector。
- [x] 首页节点 `2:724`：375x812，白底，标题 `Today's steps`，右上设置 icon；中心 166x154 步数完成度圆环，主色 `#10CEAC`，灰色轨道 `#EBEBEB` 50% 透明；进度文案为大号百分比和 `Completed`。已实现首页主结构。
- [x] 首页扩展节点 `92:364`：375x944，相比 `2:724` 增加 `Tools` 区域和 `Hydrate 400 ML` 工具卡。已实现 Tools/Hydrate 入口，数值来自当天真实饮水总量。
- [x] 数据报表日视图 `2:1384`：375x812，背景 `#F6F5F4`，顶部白色区域，白色圆角 20 数据卡，Day/Week/Month 分段控件，Day 选中黑色块 `#222222`，环形进度显示 Steps、Target、Distance、Consumption。已实现。
- [x] 数据报表周视图 `2:1137`：Week 选中；柱状图包含 Mon-Sun，柱宽 18，高度按真实步数缩放；底部统计 `Total Steps`、`Steps/Avg`、`Steps/Max`。已实现。
- [x] 数据报表月视图 `2:1250`：Month 选中；密集柱状图，柱宽约 4；横轴 `3/1`、`3/8`、`3/15`、`3/22`、`3/29`；底部统计 `Total Steps`、`Total Distance`、`Total Consumption`。已实现，横轴按当前月份动态生成。
- [x] 喝水空状态节点 `92:479`：375x922，标题 `Hydrate`，顶部返回；7 天日期横条，当前日黑底；中部大水杯插画，显示 `0 ML` 和空状态文案；主按钮 `Drink 100ML`。已实现。
- [x] 喝水记录状态节点 `92:573`：同喝水页，但底部出现记录列表，每条含水滴 icon、`200ML`、时间戳。已实现，记录来自 DataStore。
- [x] 设置页节点 `92:677`：375x812，背景 `#F9F9FA`，白色 88 顶栏，标题 `Settings`，设置项 `Language / English`、`Feedback`、`Privacy Policy`，底部版本 `V1.5.3`。已实现，并将版本配置更新为 1.5.3。
- [x] 语言选择节点 `92:720`：灰色遮罩 `#4D4D4D`，底部白色 sheet，顶部 66x8 拖拽条，标题 `Select Language`，语言列表，底部 `Cancel` / `Confirm`。已实现。

## 技术方案

- [x] 在 `app/build.gradle.kts` 增加 `alias(libs.plugins.kotlin.compose)`，开启 `buildFeatures.compose = true`。同时在根构建脚本声明 Kotlin Compose 插件。
- [x] 加入 Compose 依赖：`compose-bom`、`compose-ui`、`compose-foundation`、`compose-material3`、`compose-material-icons`、`compose-ui-tooling-preview`、`androidx-activity-compose`、`androidx-navigation-compose`、`androidx-lifecycle-viewmodel-compose`。已加入 app 模块依赖。
- [x] 加入持久化依赖：优先使用 `DataStore` 保存设置和每日步数基线；如需要喝水多条历史记录，增加 Room 或用 JSON DataStore 存储轻量列表。已加入 Preferences DataStore，后续使用 Gson JSON 存储轻量历史列表。
- [x] `MainActivity` 从 `AppCompatActivity + setContentView(XML)` 改为 `ComponentActivity + setContent { LcbStepsApp() }`。已切换。
- [x] `LcbApp` 使用普通 `Application` 作为业务入口。实机排查发现模板 `Application` 会拉起广告/指标链路并生成异常膨胀的 `track_manager` 数据库，已移除模板继承和广告/统计初始化逻辑。
- [x] 使用 edge-to-edge，Compose 内通过 `WindowInsets.statusBars`、`navigationBars` 处理顶部和底部安全区域。已使用 `statusBarsPadding` 和 `navigationBarsPadding`。
- [x] 统一 375dp 设计基准：宽度按屏幕自适应，核心间距使用设计稿 dp 值，宽屏居中限制内容最大宽度。已按 375dp 设计稿间距实现，内容随屏幕宽度填充。

## 数据与真实功能方案

- [x] 定义 `StepRepository`：封装 `SensorManager` 的 `TYPE_STEP_COUNTER`，不可用时降级 `TYPE_STEP_DETECTOR`。已实现为 `StepSensorRepository`。
- [x] 申请并处理 Android 10+ `ACTIVITY_RECOGNITION` 权限；未授权时显示真实权限状态和引导，不伪造步数。已实现权限请求与首页状态提示。
- [x] 保存每日 `stepBaseline`：第一次读取系统累计步数时记录当天基线，今日步数 = 当前累计值 - 当天基线。已在 DataStore 中保存 baseline。
- [x] 处理跨天重置：本地日期变化时重新记录基线，并把前一天数据写入历史。已在启动和传感器写入时执行 rollover。
- [x] 定义 `StepDailyRecord(date, steps, goal, distanceKm, calories)`；距离按用户步幅或默认步幅估算，热量按步数/距离估算。已定义记录与 `StepMetrics` 估算。
- [x] 定义 `HydrationRecord(id, dateTime, amountMl)`；点击 `Drink 100ML` 立即新增记录并更新当天总饮水量。已实现。
- [x] 保存 `goalSteps`、`language`、`waterQuickAmountMl`、`versionName` 等设置项。已保存目标、语言和快捷饮水量；版本来自 BuildConfig。
- [x] 报表 Day/Week/Month 读取真实历史记录；没有历史时显示 0 值和空图表，不填充 mock。已实现。

## UI 复刻方案

- [x] 建立设计 token：`Primary #10CEAC`、`PrimaryBar #43E0C4`、`TextPrimary #222222/#333333`、`TextSecondary #666666`、`TextTertiary #999999`、`CardGray #F5F5F5`、`PageGray #F6F5F4`、`SettingsBg #F9F9FA`。已集中在 `ui/theme/Theme.kt`。
- [x] 字体策略：系统可用时使用 Roboto；MiSans 不随系统保证存在，使用 bundled font 或以 FontFamily.SansSerif 兜底，并保持字重/字号。当前使用系统 sans/Roboto 兜底并匹配字重字号。
- [x] 复刻图标：优先用 Compose VectorDrawable/自定义 `ImageVector` 实现 home、data、settings、distance、step、kcal、hydrate、back、more、water-drop。已用 Canvas 自定义绘制。
- [x] 复刻 logo：生成 adaptive icon foreground/background；背景 `#10CEAC`，白色运动矢量和圆环。已实现。
- [x] 首页圆环：用 Canvas 绘制灰色轨道和主色圆弧，stroke 16dp，中心显示完成百分比。已实现。
- [x] Fitness Data 卡片：3 个 343x66、圆角 16、背景 `#F5F5F5` 的数据行，左侧 42x42 白底 icon 容器。已实现。
- [x] Tools/Hydrate 卡片：按 `92:364` 增加 `Tools` 标题和浅蓝 `#F2F7FF` Hydrate 入口卡，右侧更多箭头。已实现。
- [x] 底部 Tab：白色 49dp 高，阴影半径 4，两个 tab，Home/Data 状态切换颜色 `#222222` / `#999999`。已实现。
- [x] 数据报表：实现顶部标题、分段控件、日期切换箭头、Day 圆环、Week/Month 柱状图和统计区。已实现核心报表。
- [x] 喝水页：实现日期横条、水杯插画、当天总量、按钮、记录列表和空状态。已实现。
- [x] 设置页：实现顶栏返回、设置项列表、版本号、语言选择 BottomSheet。已实现。

## 页面与模块拆分

- [x] `ui/App.kt`：Compose 根入口、主题、NavHost。已实现。
- [x] `ui/theme/*`：颜色、字体、尺寸 token。已实现。
- [x] `ui/home/HomeScreen.kt`：首页计步、Fitness Data、Tools、底部 tab。已实现。
- [x] `ui/report/ReportScreen.kt`：Day/Week/Month 报表。已实现。
- [x] `ui/hydrate/HydrateScreen.kt`：喝水详情和记录。已实现。
- [x] `ui/settings/SettingsScreen.kt`：设置页和语言 sheet。已实现。
- [x] `ui/components/*`：状态栏占位、底部 tab、圆环进度、柱状图、设置行、数据卡、矢量 icon。已实现为 `DesignComponents.kt`。
- [x] `data/steps/*`：传感器监听、每日基线、历史统计。已实现为 `StepSensorRepository` + `AppPreferences`。
- [x] `data/hydration/*`：喝水记录持久化与查询。已实现为 `HydrationRecord` + DataStore JSON。
- [x] `data/settings/*`：语言、目标步数、快捷喝水量。已实现为 DataStore preferences。

## 执行清单

- [x] 复制模板项目到当前目录，并确认 Gradle Sync 基础可用。已建立模板基线，后续通过 Gradle 构建验证同步可用性。
- [x] 修改项目名、应用名、包名、versionName、launcher icon 资源。已改项目名、应用名、versionName 和 launcher icon；包名/应用 ID 保持模板配置以避免 Google Services 配置失配。
- [x] 开启 Compose 构建配置并移除首页 XML 依赖。Compose 已开启，后续 `MainActivity` 将不再调用 XML 布局。
- [x] 接入主题 token、字体、全局尺寸。已实现。
- [x] 实现 Compose 导航：Home、Report、Hydrate、Settings。已实现。
- [x] 实现计步权限申请和传感器监听。已实现。
- [x] 实现每日步数基线、跨天归档、目标步数保存。已实现。
- [x] 实现首页 UI，先完成 `2:724`，再补齐 `92:364` 的 Tools/Hydrate 区。已实现。
- [x] 实现 Hydrate 入口跳转和饮水记录真实写入。已实现。
- [x] 实现喝水空状态 UI `92:479`。已实现。
- [x] 实现喝水有记录 UI `92:573`。已实现。
- [x] 实现数据报表 Day UI `2:1384`。已实现。
- [x] 实现数据报表 Week UI `2:1137`，柱状图来自真实周数据。已实现。
- [x] 实现数据报表 Month UI `2:1250`，柱状图来自真实月数据。已实现。
- [x] 实现设置页 UI `92:677`。已实现。
- [x] 实现语言选择 BottomSheet `92:720`。已实现。
- [x] 实现 Feedback 跳转：优先邮件 intent 或系统分享/反馈入口。已实现邮件 intent。
- [x] 实现 Privacy Policy 跳转：使用配置 URL；未配置时显示不可用提示。已实现浏览器 intent，当前使用占位 URL。
- [x] 完成无传感器、无权限、首次安装、跨天、无记录等状态处理。已实现权限/无传感器提示、首次 0 值、跨天 rollover、喝水空状态。
- [x] 添加 ViewModel/Repository 单元测试：步数计算、跨天重置、饮水统计、报表聚合。已添加 `StepMetricsTest` 覆盖步数完成率、百分比、距离和热量估算；DataStore/传感器集成留待仪器测试。
- [x] 在模拟器和真机验证：权限流程、传感器数据、UI 适配、深色模式禁用或保持白底。已在连接设备 `6aa8820f` 安装并显式启动 `com.example.lcb.app.MainActivity`，确认首页 Compose UI 可见且无业务崩溃；真实步数变化需携带设备走动后继续观察。
- [x] 执行 `./gradlew :app:assembleLocalDebug` 验证构建。首次增量 `packageLocalDebug` 中断，重跑同一任务后 `assembleLocalDebug` 成功，APK 位于 `app/build/outputs/apk/local/debug/app-local-debug.apk`。

## 风险与处理

- [x] 计步传感器不是所有设备都有：无 `TYPE_STEP_COUNTER` 时使用 `TYPE_STEP_DETECTOR`，仍不可用时显示设备不支持，不显示假数据。已实现。
- [x] Android 10+ 权限被拒绝：页面显示 0 和权限入口，报表只展示已记录历史。已实现。
- [x] MiSans 字体授权和文件来源未提供：实现时优先用系统 Roboto/默认 sans，若提供字体文件再加入 `res/font`。已使用系统字体兜底。
- [x] Figma 中部分语言选择节点包含无关背景内容：实现以底部语言 sheet 为准，背景使用当前设置页遮罩。已实现。
- [x] 设计稿为 iOS 状态栏样式：Android 实现只保留安全区和视觉高度，不绘制假的系统电量/Wi-Fi 状态。已实现。

## 验收标准

- [x] 首页、报表、喝水、设置、语言 sheet 与 Figma 主要布局、颜色、字号、圆角、间距一致。已按 Figma 结构和 token 实现，设备启动验证首页可见。
- [x] 首页步数、完成率、距离、热量随真实传感器数据变化。已接入传感器和派生指标；实机走动后由系统传感器回调驱动。
- [x] 喝水按钮点击后立即新增真实记录，退出重进仍保留。已通过 DataStore 持久化。
- [x] Day/Week/Month 报表由本地历史数据聚合生成。已实现。
- [x] 无权限和无传感器设备有明确状态，不出现伪造数据。已实现。
- [x] 本地构建通过，主流程无崩溃。`compileLocalDebugKotlin`、`testLocalDebugUnitTest`、`assembleLocalDebug` 均通过；设备显式启动首页无崩溃。

## 优化记录 2026-04-24

- [x] 所有业务 Activity 只允许竖屏。已在 `app/src/main/AndroidManifest.xml` 为 `MainActivity` 增加 `android:screenOrientation="portrait"`；模板 `bill` 模块已有 Activity 保持 portrait。
- [x] 首页图标从 Figma 精确导出并接入：右上角 Settings、Fitness Data 的 Distance、Steps、Kcal。已读取 Figma 首页 `2:724`，定位 `91:343`、`2:794`、`2:806`、`2:817`，将 Figma 导出的原始 SVG 资产渲染为 PNG drawable，并替换 Compose 手绘近似图标。
- [x] 喝水页主水杯图标替换为本地 `飞书20260424-154932.gif`，并保持喝水记录真实写入。已复制为 `app/src/main/res/drawable-nodpi/water_cup.gif`，在 Hydrate 页面用 Glide + `AndroidView` 播放 GIF，原有 `Drink 100ML` 持久化逻辑未改动。
- [x] 优化验证。已执行 `./gradlew :app:testLocalDebugUnitTest :app:assembleLocalDebug`，构建和单元测试通过；已安装到设备 `6aa8820f` 并启动 `MainActivity`，当前焦点为应用首页。
- [x] 首页低步数完成率显示优化。确认 `50 / 8000` 的真实进度只有 `0.625%`，圆环弧长很短属于正常；已新增 `percentText`，低于 10% 时最多显示 1 位小数，并根据字符串长度动态调整字号，避免百分比文本撑破圆环。
- [x] 首页喝水入口前置。已将喝水入口从首页底部 Tools 区迁移到标题栏，放在 Settings 左侧，并在图标右上角增加 8dp 红点提醒；点击仍进入真实 Hydrate 页面。
- [x] 本轮优化验证。已执行 `./gradlew :app:testLocalDebugUnitTest :app:assembleLocalDebug`，构建和单元测试通过；已安装到设备 `6aa8820f` 并显式启动 `MainActivity`。
- [x] 设置页语言文案国际化。已将 Settings 页面标题、设置项、语言选择弹窗、Cancel/Confirm、语言国家名称迁移到 `strings.xml`，并新增 `values-zh-rCN` 中文资源；语言选择持久化改为稳定 code，同时兼容旧版已保存的显示文案。
- [x] 设置页国际化语法验证。已用 `xmllint` 校验默认和中文 `strings.xml`；已执行 `./gradlew :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug`，资源编译、Kotlin 编译、单测和 APK 构建通过；已安装到设备 `6aa8820f` 并显式启动 `MainActivity`。
- [x] 语言选择弹框所有语言项补齐多语言资源。已新增 `values-de`、`values-es`、`values-fr`、`values-pt`、`values-ja`、`values-ko`，覆盖 Settings 页面文案和弹框内 English/Deutsch/Español/Français/Português/日本語/한국어/简体中文 全部语言项。
- [x] 语言资源补齐验证。已确认所有 `strings.xml` 均包含 8 个 `language_*` 条目，`xmllint` 校验通过；已执行 `./gradlew :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug`，资源编译、Kotlin 编译、单测和 APK 构建通过。
- [x] 首页、喝水页、Data 页字符串国际化。已将 Home、Hydrate、Report 和底部 Tab 的标题、按钮、状态提示、统计标签、单位文案、星期标签迁移到 `stringResource`，并同步补齐 8 套语言资源。
- [x] 首页/喝水/Data 国际化验证。已确认 8 套 `strings.xml` 均包含 45 个 `home_`、`hydrate_`、`report_`、`nav_`、`weekday_` 条目，`xmllint` 校验通过；已执行 `./gradlew :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 并安装到设备 `6aa8820f` 显式启动 `MainActivity`。
- [x] 语言切换实时刷新。已在 Compose 根节点按已选择语言 code 提供 localized `Context` 和 `Configuration`，`stringResource`、日期和星期会随 DataStore 中的语言变化立即重组刷新。
- [x] 今日距离显示确认与优化。距离公式为 `steps * 0.00057` km，4200 步约 2.394 km，显示为 2.4 KM，符合 Figma 示例；50 步约 0.0285 km，首页 1KM 以下改为两位小数显示，避免显示成 0.0 KM。
- [x] 语言实时刷新与今日距离最终验证。已执行 `xmllint`、`./gradlew :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug`，并安装最新 APK 到设备 `6aa8820f` 显式启动 `MainActivity`，当前焦点为应用主界面。
- [x] 页面返回与底部 Tab 卡顿排查。确认原实现存在根节点订阅完整 `AppData`、Data tab 回 Home 销毁重建首页、喝水 GIF 在 `AndroidView.update` 中重复 Glide 加载、喝水记录列表一次性组成等问题，属于不够符合 Compose 性能实践的实现方式。
- [x] 页面返回与底部 Tab 流畅度优化。已拆分 `language/homeData/reportData/hydrateData` 页面级 Flow 并使用 `collectAsStateWithLifecycle`，NavHost 禁用默认转场，Settings/Hydrate 防重复入栈，Data tab 返回 Home 改为保留已有首页，喝水页改为 `LazyColumn` 并让 GIF 只在 View 创建时加载，计步传感器写入增加相同总步数去重并降为 `SENSOR_DELAY_NORMAL`。
- [x] 流畅度优化验证。已执行 `./gradlew :app:compileLocalDebugKotlin`、`./gradlew :app:testLocalDebugUnitTest :app:assembleLocalDebug`，并安装到设备 `6aa8820f` 显式启动；通过 adb 执行 Home/Data、Hydrate 返回、Settings 返回路径并读取 `dumpsys gfxinfo`，常规路径无崩溃，GPU 帧耗时低，剩余少量 jank 主要来自 debug 构建和首次页面创建的 UI thread 开销。
- [x] LeakCanary 分析 OOM 与 CursorWindow NO_MEMORY 排查。结合实机日志和 `dumpsys meminfo`，确认不是 Compose 列表本身导致，而是模板广告/指标 SDK 链路引入多个 WebView、模板 Activity，并生成约 500MB 级 `track_manager_metrics_component.db`，导致主进程 Java Heap 接近 512MB 上限，LeakCanary 在分析巨大 heap dump 时再次 OOM。
- [x] 内存泄漏/内存暴涨修复。已将 `LcbApp` 从模板 `Iej9ieio6r89e7ya` 改为普通 `Application`，移除 `maxquicklitememory`、AdjustTracker 初始化和模板返回页能力；在 app 模块移除 `implementation(project(":bill"))`、`implementation(project(":core"))`、`implementation(project(":metrics"))`，使广告 SDK、TopOn/Pangle/IronSource、模板 Activity 和相关 ContentProvider 不再合并进 APK；启动时清理历史遗留的 `track_manager_metrics_component.db`、`track_manager_metrics_sdk.db`、`track_manager_monitor.db` 及 wal/shm/journal 文件。
- [x] 内存修复验证。已执行 `./gradlew :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 并通过；新的合并 Manifest 仅剩业务 `MainActivity`、Firebase、AndroidX Startup 和 LeakCanary，已确认不再包含 `com.leafmotivation.*` 模板 Activity、广告 Provider、`track_manager` 相关组件。已安装到设备 `6aa8820f` 并启动，`dumpsys meminfo` 显示 `TOTAL PSS` 约 186MB、Java Heap 约 21MB、WebViews 为 0，最近日志未再命中 `CursorWindow NO_MEMORY`、`OutOfMemory`、`MirrorFolderActivity`、`track_manager`、IronSource/TopOn/Pangle 相关输出。
- [x] 同步 LCB_Template 最新广告提交。已读取 `/Users/apple/AndroidStudioProjects/LCB_Template` 最新提交 `9b7d0ea 广告初始化、广告扩展方法`，同步 `LcbAdInitializer`、默认 AdMob/Pangle/TopOn 原生广告和全屏原生广告渲染器、广告 Loading 渲染器、`BusinessAdExt` 扩展方法，以及新增广告布局和 drawable 资源；`LcbApp.onCreate()` 已调用 `LcbAdInitializer.initialize(this)`，`app/build.gradle.kts` 已补充 `androidx.cardview:cardview:1.0.0`。
- [x] 广告同步验证。已执行 `./gradlew :app:compileLocalDebugKotlin`、`./gradlew :app:testLocalDebugUnitTest :app:assembleLocalDebug` 并通过；已安装到设备 `6aa8820f` 启动 `MainActivity`，应用进程存在，最近日志未命中 `FATAL EXCEPTION`、`AndroidRuntime` 或 `Failed to initialize ads`。
- [x] 广告位接入方案。已将 `MainActivity` 从 `ComponentActivity` 调整为 `FragmentActivity` 以直接使用模板广告扩展方法；新增 Compose `NativeAdSlot` 封装 `loadNative`，在 Home 的 Fitness Data 下方和 Data 报表卡下方接入原生广告；Hydrate 和 Settings 作为低频二级页入口，点击进入时通过 `loadInterstitial` 尝试展示插屏，并增加 90 秒冷却和间隔入口控制，避免底部 tab、喝水按钮、语言弹框等高频操作被插屏打断。
- [x] 广告位接入验证。已执行 `./gradlew :app:compileLocalDebugKotlin`、`./gradlew :app:testLocalDebugUnitTest :app:assembleLocalDebug` 并通过；已安装到设备 `6aa8820f` 启动 `MainActivity`，应用进程存在，最近日志未命中 `FATAL EXCEPTION`、`AndroidRuntime`、`ClassCastException`、`Failed to initialize ads`、`loadNative` 或 `loadInterstitial` 相关异常。
- [x] Crashlytics build ID 缺失修复。排查确认 `firebase-crashlytics`/`firebase-crashlytics-ndk` 运行时库已由模板链路合并进 APK，但 app 模块未应用 `com.google.firebase.crashlytics` Gradle 插件，导致运行时缺少 Crashlytics build ID；已在根 `build.gradle.kts` 声明 Crashlytics 插件，在 `app/build.gradle.kts` 应用该插件，并显式加入 `libs.firebase.crashlytics` 依赖，避免只依赖模板库传递。
- [x] Crashlytics 修复验证。已执行 `./gradlew :app:compileLocalDebugKotlin :app:assembleLocalDebug` 并通过；构建中已出现 `injectCrashlyticsMappingFileIdLocalDebug` 任务，生成资源 `app/build/generated/res/injectCrashlyticsMappingFileIdLocalDebug/values/com_google_firebase_crashlytics_mappingfileid.xml`，最终资源表包含 `com.google.firebase.crashlytics.mapping_file_id`。
- [x] Crashlytics 修复同步到 `LCB_Template`。已在 `/Users/apple/AndroidStudioProjects/LCB_Template/build.gradle.kts` 声明 `google.firebase.crashlytics` 插件，在模板 `app/build.gradle.kts` 应用该插件并显式加入 `libs.firebase.crashlytics` 依赖，使后续基于模板新建的业务 App 默认生成 Crashlytics build ID。
- [x] `LCB_Template` Crashlytics 同步验证。已在模板工程执行 `./gradlew :app:compileLocalDebugKotlin :app:assembleLocalDebug` 并通过；构建中已运行 `:app:injectCrashlyticsMappingFileIdLocalDebug`，生成 `app/build/generated/res/injectCrashlyticsMappingFileIdLocalDebug/values/com_google_firebase_crashlytics_mappingfileid.xml`，资源表包含 `com.google.firebase.crashlytics.mapping_file_id`。
- [x] 原生广告加载方式简化。已移除 Compose `NativeAdSlot` 外层自维护的 `visible` 状态、1dp/GONE 容器、成功失败回调和额外隐藏逻辑，广告容器创建后直接调用 `loadNative`；`loadNative` 扩展也改为只转调 `AdShowExt.showNativeAdInContainer`，显示/隐藏交由广告 SDK 内部控制。
- [x] 原生广告简化验证。已执行 `./gradlew :app:compileLocalDebugKotlin` 和 `./gradlew :app:assembleLocalDebug`，Kotlin 编译和 APK 构建均通过。
- [x] 历史数据迁移到 Room。已新增 `lcb_steps.db`，使用 `daily_steps` 表保存按天步数历史、`hydration_records` 表保存喝水明细；DataStore 只继续保存今日步数、目标步数、计步器基线、语言、快捷喝水量等基础配置。旧版 DataStore 中的 `step_history` 和 `hydration_records` JSON 会在首次启动/首次写入时自动迁移到 Room，迁移完成后删除旧 JSON key 并写入 `room_history_migrated` 标记。
- [x] Room 数据容量控制与验证。Room 写入仍保留上限控制：步数历史最多 400 天，喝水明细最多 500 条；喝水记录主键改为毫秒时间加纳秒尾号，避免连续点击覆盖同毫秒记录。已执行 `./gradlew :app:compileLocalDebugKotlin`、`./gradlew :app:testLocalDebugUnitTest :app:assembleLocalDebug`，Room KSP、单测和 APK 构建均通过。
- [x] 底部 Home/Data tab 广告保活。确认原实现用 Navigation destination 切换主 tab，Home/Data 离开 composition 时会触发 `AndroidView.onRelease` 并移除广告 View；已改为主 tab keep-alive 层，Home 常驻，Data 首次打开后常驻，只切 `alpha/zIndex`，避免底部 tab 来回切换释放原生广告容器。
- [x] 原生广告本地调试源修正与验证。已修复 `NativeAdSlot` 从 localized `Context` 找不到 `FragmentActivity` 的问题，改用 `LocalView.current.context` 获取真实 Activity；`loadNative` 改为传 Activity context；初始化后预加载广告。实机日志已确认出现 `原生广告竞价开始`、`使用固定源: ADMOB`、`使用 AdMob 展示原生广告`、`Admob使用缓存中的原生广告`；由于 local 配置里的 Pangle/TopOn 测试参数失败，local flavor 已固定使用并只预加载 AdMob 源，正式 google flavor 仍走原聚合预加载。
- [x] 首页首屏布局收紧与广告可见性优化。已将 Home 顶栏、圆环进度区、目标摘要卡和 Fitness Data 间距整体压缩，并把首页原生广告从 Fitness Data 下方前移到目标摘要卡下方，使广告更容易在首屏真实可见；`NativeAdSlot` 增加 `active` 控制，Home/Data keep-alive 隐藏页会将广告 View 设为 `INVISIBLE` 且不触发新加载，降低不可见广告展示风险。
- [x] 首页广告可见性优化验证。已执行 `./gradlew :app:compileLocalDebugKotlin :app:assembleLocalDebug`，Kotlin 编译和 APK 构建通过。
- [x] 原生广告容器高度与设置页插屏调整。已将 Compose `NativeAdSlot` 从固定 `84dp` 高度改为 `wrapContentHeight()`，由广告 SDK 实际渲染内容决定高度；首页 Settings 入口改为直接 `navigate(Routes.Settings)`，不再触发插屏广告，保留 Hydrate 入口插屏策略。
- [x] 广告容器与 Settings 插屏调整验证。已执行 `./gradlew :app:compileLocalDebugKotlin :app:assembleLocalDebug`，Kotlin 编译和 APK 构建通过。
- [x] 业务点击埋点接入。已使用 `ReportDataManager.reportData` 接入四个点击事件：`click_enter_hydrate` 首页进入喝水页按钮、`click_drink_water` 喝水页喝水按钮、`click_tab_data` 底部 Data 导航、`click_tab_home` 底部 Home 导航；底部 tab 即使点击当前已选中项也会上报。
- [x] 业务点击埋点验证。首次普通 Gradle daemon 在打包阶段因 2GB heap GC thrashing 被停止，非代码编译错误；随后使用 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 验证通过。
- [x] App Logo 从 Figma 重新完整导出。已用 Figma 插件读取 `dVsTL6ggoDPXVPcEKg856G` 的 `95:986` 节点，确认其为 512x512 完整 logo；下载资产 `a67d684c-4cde-4120-8cd3-a196527776be`，生成 `drawable-nodpi/ic_launcher_logo.png`，并按 mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi 重新生成 `ic_launcher.png` 与 `ic_launcher_round.png`。
- [x] Adaptive launcher logo 同步。已将 `ic_launcher_foreground.xml` 改为 Figma SVG 的真实鞋子、圆环路径，`ic_launcher_background.xml` 保持 Figma 背景色 `#10CEAC`，让 Android 26+ 启动图标也使用同一套 Figma logo。
- [x] App Logo 资源验证。已用本地图片预览确认源图与 Figma 截图一致；已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug`，资源编译、Kotlin 编译和 APK 构建通过。
- [x] GoStep 配置 JSON 安全 ASCII 编码处理。已对照 `../lcb_pdf/sdk_res/config.json`，将 `lcb_res/GoStep Walking Tracker config.json` 使用 `ensure_ascii` 重新输出，文件字节层面为 ASCII-only；法语等带重音语言保留原翻译语义并以 Unicode escape 存储。
- [x] 越南语特殊处理。参考 PDF 配置中 `vi` 为去声调 ASCII 文案，已将 GoStep 配置的 `string.vi` 全量去声调，并将越南语列表项目符号 `•` 替换为 ASCII `-`，使越南语解码后也不含非 ASCII 字符。
- [x] GoStep 配置 JSON 验证。已执行 `python3 -m json.tool` 解析通过；确认 `base/de/es/fr/id/in/ko/pt/ru/vi` 每种语言均为 117 个 string 条目，文件 raw bytes 全部 `< 128`，`vi` 解码后非 ASCII 条目数为 0。
- [x] GoStep 配置乱码风险复核。已验证 JSON raw bytes 全部为 ASCII，按 UTF-8/GBK/ISO-8859-1 直接读取原始文件不会产生“奇怪中文”类 mojibake；JSON 解析后法语样例为 `Définir comme launcher par défaut`、`Paramètres système`、`Étape 1 :`，不存在替换字符 `�`；越南语样例为 `Dat lam launcher mac dinh`、`Cai dat he thong`、`Buoc 1:`，全部为 ASCII，避免声调字符显示成 `?`。
- [x] GoStep 配置俄语按钮文案缩短。已在 `lcb_res/config.json` 中将 `string.ru.novi_contact_us` 从 `Связаться с нами` 缩短为 `Связь`，用于按钮时长度从 15 个字符降到 5 个字符，降低换行风险；JSON 仍保持 ASCII-only 转义格式，`python3 -m json.tool` 校验通过，俄语条目数保持 117。
- [x] app 模块只打包 armv8a/arm64-v8a。已在 `app/build.gradle.kts` 的 `defaultConfig.ndk` 中加入 `abiFilters += "arm64-v8a"`，让 app 只打包 Android armv8a 对应的 64 位 ARM ABI。
- [x] arm64-v8a 打包验证。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:assembleLocalDebug` 构建通过；检查 `app/build/outputs/apk/local/debug/app-local-debug.apk` 中 `lib/` 目录，确认唯一 ABI 为 `arm64-v8a`。
- [x] 当前项目初始化 Git 仓库。已在 `/Users/apple/StudioProjects/lcb_countsteps` 执行 `git init -b main`，创建本地 Git 仓库并使用 `main` 分支，和 GitHub Action 的 main 分支触发配置保持一致。
- [x] 本地 GitHub 凭据文件忽略。已将 `build.config.properties` 加入 `.gitignore`，并用 `git check-ignore -v build.config.properties` 确认命中忽略规则；该文件内的 GitHub 凭据不进入 Git，CI 侧通过 GitHub Secrets/Actions 环境变量提供。
- [x] GitHub Action 自动生成 google release 签名文件复核。当前本地 `app/src/google/google-release.keystore` 不存在；已确认 workflow 的 `Ensure google release keystore exists` 会在缺失时用 `keytool -genkeypair` 生成 `app/src/google/google-release.keystore` 并提交回 `main`。已用同一组参数在临时目录生成 PKCS12 keystore 并用 `keytool -list` 验证 alias `google` 可读。
- [x] Gradle 签名路径链路复核。已临时按 Action 路径生成 `app/src/google/google-release.keystore`，并带 `ANDROID_SIGNING_STORE_FILE=app/src/google/google-release.keystore` 执行 `./gradlew --no-daemon -q :app:printGoogleReleaseVersionName :app:printGoogleReleaseAabName --console=plain`，输出 `1.5.3` 和 `lcb_GoStep_release_1.5.3.aab`，说明生成后的签名路径能被 Gradle 识别；测试后已删除临时 keystore，保持当前本地缺省状态。
- [x] app 模块混淆规则补齐。已扫描 app 代码，确认不处理广告 SDK 自带 consumer rules，只在 `app/proguard-rules.pro` 补 app 自己的规则：Manifest 入口 `LcbApp/MainActivity`、Gson 旧 DataStore JSON 迁移模型 `StepDailyRecord/HydrationRecord`、Room 数据库/实体/DAO/生成实现、以及 app 自己传入广告 SDK 的 renderer 回调类。
- [x] app 模块现有编译阻塞修复。静态扫描和本地构建发现当前项目漏了模板依赖 `com.launcher.unity:com.leafmotivation.quizguessoncolor-dev:1.0.1`，导致 `LcbApp` 父类解析失败；同时 Pangle/TopOn 普通原生 renderer 接口已变为带 style 参数，`layout_native_ad_card.xml` 仍使用旧 AdMob `NativeAdView` 类。已补依赖、修正 renderer 方法签名和布局根类。
- [x] app 混淆规则本地验证。未运行 google 线上任务；已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:assembleLocalDebug`，本地 `localDebug` 构建通过。
- [x] GitHub Packages 私有 Maven 凭据读取优化。已将 `settings.gradle.kts` 改为优先读取本地 `build.config.properties` 的 `github.user/github.token`，CI 下优先读取仓库 Secrets 映射的 `GH_PACKAGES_USER/GH_PACKAGES_TOKEN`，最后回退到 `GITHUB_ACTOR/GITHUB_TOKEN`；workflow 已增加 `GH_PACKAGES_USER` 和 `GH_PACKAGES_TOKEN` 环境变量，未运行 google 打包任务。
- [x] 首页黑色主题重构。已使用 Figma 节点 `2150:281` 重构 HomeScreen：背景改为 `#111113`，主卡改为 `#1C1C22`，顶部保留 Settings，步数卡展示今日真实步数、可编辑今日总步数、目标进度渐变条、Daily avg 和一周 Report 状态；一周状态来自 Room 步数历史，0 步显示 `x`，达到目标显示 `✓`，有数据未达目标显示圆形进度。
- [x] 首页底部 Health Tools 入口。已按 Figma 增加 `Today’s Exercise`、`Health Tools`、Log water、Log weight、Achievements 三张卡；喝水和体重按本轮范围暂不接入业务数据/跳转，仅保留视觉入口，成就卡按设计展示静态进度文案。
- [x] 首页底部 tab 暗色适配。`AppBottomBar` 新增暗色参数，Home 页使用黑色 tab 和 `Trends` 标签，Data 页默认浅色样式不受影响。
- [x] 首页重构验证。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug`，Kotlin 编译、单测和 local debug APK 构建通过。
- [x] 首页 Figma 精修。重新使用 Figma `get_metadata` 和 `get_design_context` 解析 `2150:281` 的完整坐标树，并按 375dp 设计基准调整顶部、步数卡、Exercise 卡、Health Tools 和底部 tab 的尺寸/间距；导出 Settings、Edit、Pause、More、Report 状态、Exercise、水滴、锁、体重、奖章、徽章、Home/Trends tab 等原始 Figma 资产，预处理 SVG 的 `var(--fill-*)` 颜色后转换为透明 PNG，替换上一版手绘近似图标。
- [x] 首页步数编辑修正。根据 Figma 中 edit 图标位于今日总步数右侧，已将编辑行为改为修改今日总步数，并通过 DataStore + Room 同步当天步数记录；每日目标仍作为 `Goal` 展示和周进度判定目标。
- [x] 首页实机视觉验证。已执行 `:app:assembleLocalDebug`、安装到设备 `6aa8820f`，显式启动 `com.example.lcb.app.MainActivity` 并截图检查；修复了 Android 状态栏深色图标叠黑底的问题，`MainActivity` 的 edge-to-edge system bar 已改为 dark style。
- [x] 首页高清图标重导出。重新通过 Figma `get_design_context` 读取 `2150:281`，使用 Figma 返回的原始 SVG/图片 asset 下载地址，重新生成 Settings、Edit、Pause、More、Exercise、水滴、锁、体重、奖章、徽章、Home/Trends tab 等首页实际显示资源；所有保留的 `home_dark_*.png` 均为 512x512 透明 PNG，替换原 96/128px 资源，降低高密度屏模糊风险。
- [x] Report 周状态改为自定义 View。已删除周报状态图片依赖，`WeeklyReport` 仍沿用 Room 步数历史和今日真实步数判定状态，`DayStateIcon` 改为 Compose `Canvas` 自绘：无数据显示灰色圆 + X，已完成显示蓝色圆 + √，有数据未完成显示灰色轨道 + 蓝色进度弧。
- [x] 首页高清图标与周状态验证。已确认 `home_dark_complete_*` 不再有引用，剩余首页图标资源均为 512x512；已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug`，Kotlin 编译、单测和 local debug APK 构建通过。已安装到设备 `6aa8820f` 并截图 `/tmp/lcb_home_hd_report.png`，确认高清图标正常显示，Report 周状态由自绘圆、X、进度弧渲染。
- [x] 首页步数卡无效入口清理。已移除第一个卡片中没有实际业务逻辑的暂停图标和更多图标，并删除对应未使用资源；编辑入口从大步数右侧移动到 `Goal` 文案后方，保持和 12sp 目标文本垂直居中，点击仍复用原来的今日步数编辑逻辑。
- [x] 首页 Report 星期缩写修正。确认原来不是数据错误，而是 21dp 文本列宽导致 `Sun/Mon/Wed/Thu` 被裁成 `Su/Mo/We/Th`；已将周状态 item 宽度调整为 32dp，并改为复用 `weekday_*` 国际化资源。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过；已安装到设备 `6aa8820f` 并截图 `/tmp/lcb_home_weekday_3letters.png`，确认显示 `Sun/Mon/Tue/Wed/Thu/Fri/Sat`。
- [x] 首页每日进度条视觉优化。已将第一个卡片里的每日进度条从双 `Box` 裁剪改为 Compose `Canvas` 自绘圆角矩形，轨道和前景进度都使用半高度圆角；高度从 12dp 提升到 14dp，非零低进度增加最小胶囊宽度，避免 40/10000 这类数据只显示成细线。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过；已安装到设备 `6aa8820f` 并截图 `/tmp/lcb_home_progress_pill.png`，确认低进度显示为圆角进度状态。
- [x] 首页每日进度条厚度微调。根据实机视觉反馈，将第一个卡片里的每日进度条高度从 14dp 提升到 16dp，并同步微调上下 spacer，保持卡片内部总高度稳定；圆角和低进度最小胶囊逻辑不变。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过。
- [x] 首页 Compose 组件拆分。已将原来 778 行的 `HomeScreen.kt` 拆分为职责明确的同包文件：`HomeScreen.kt` 只保留页面组装与弹窗状态，`HomeChrome.kt` 承载顶部栏/权限提示/底部 tab，`HomeStepCard.kt` 承载步数卡/每日进度/周报/步数编辑弹窗，`HomeHealthTools.kt` 承载运动卡和 Health Tools，`HomeTokens.kt` 集中首页暗色设计 token；外部 `HomeScreen` 调用签名不变，业务数据和 UI 行为不变。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过。
- [x] 首页 Report 每日状态进度环加粗。确认 Report 一周状态中的进度态不是 view 尺寸问题，而是 `DayStateIcon` 的圆环 stroke 仅 1.4dp；已将进度态轨道/蓝色弧线 stroke 调整为 3dp，并为非零低进度设置最小 8 度可见弧长，避免 40/10000 这类数据只显示成极细点。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过；已安装到设备 `6aa8820f` 并截图 `/tmp/lcb_home_report_ring_thick.png`，确认 Report 小圆进度环明显加粗。
- [x] 首页 Detail 箭头尺寸调整。已将第一个卡片 `Detail` 右侧箭头图标从 18dp 缩小到 14dp，并增加 2dp 文本间距，让箭头作为辅助指示而不是视觉主元素。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过。
- [x] 首页 Goal 编辑弹框改为独立 BottomSheet。已新增 `HomeEditStepsSheet.kt`，用 Material3 `ModalBottomSheet` 实现顶部圆角、圆角拖拽手柄、深色容器和高对比输入框；输入只允许数字并限制 7 位，确认按钮在输入无效时禁用，避免把非法目标写入持久化层。
- [x] 首页编辑逻辑更正为修改每日目标步数。最终行为已从“修改今日已走步数”改为“修改目标步数”，首页仍显示真实传感器步数不可篡改；`HomeScreen` 只调用 `viewModel.setStepGoal`，弹框文案改为 `Set step goal` / `Daily step goal`，实机验证弹框显示正确。
- [x] 设置页改为独立 Activity。已新增 `SettingsActivity` 并在 Manifest 注册为 `android:screenOrientation="portrait"`、`android:exported="false"`；首页设置按钮现在通过 `Intent(context, SettingsActivity::class.java)` 打开，`NavHost` 中移除 Settings route；设置页继续使用独立 ViewModel 读取语言并提供 localized `Context` / `Configuration`，语言切换仍可实时刷新当前 Activity。
- [x] 黑色主题扩展到主要页面。已在 `ui/theme/Theme.kt` 增加全局深色 token，并将 Settings、Data/Trends、Hydrate 页面的背景、卡片、分隔线、文字、返回箭头和底部 tab 切到与首页一致的黑色体系；`BackButton` 增加颜色参数，避免深色页复用浅色箭头导致对比异常。
- [x] 设置页 Activity 与主题验证。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过；已安装到设备 `6aa8820f`，从首页点击设置按钮后 `topResumedActivity` 为 `com.example.lcb.app.SettingsActivity`，截图 `/tmp/lcb_settings_activity_dark.png` 确认设置页黑色主题正常；截图 `/tmp/lcb_goal_bottom_sheet.png` 确认目标编辑 BottomSheet 文案和输入框视觉正常。
- [x] 喝水页 Figma 新版解析。已用 Figma 工具读取 `2171:735` 无数据和 `2171:787` 有数据节点：页面 375x812，黑色背景 `#111113`，顶部 578dp 氛围背景，Toolbar 区 44dp，进度文案位于 y=112，水滴控件 240x240 位于 y=236，主按钮 215x48 位于 y=559，Done 按钮 215x48 位于 y=637；有数据状态通过水滴内波浪水位表达。
- [x] 喝水页改为独立 Activity + 原生 Toolbar。已新增 `HydrateActivity` 并在 Manifest 注册竖屏、`exported=false`；首页 Log water 入口现在启动该 Activity，主 NavHost 中移除旧 Hydrate route；顶部使用 `androidx.appcompat.widget.Toolbar`，通过 `setSupportActionBar` 和 `setDisplayHomeAsUpEnabled(true)` 使用 Toolbar 自带返回按钮 API。
- [x] 喝水自定义控件实现。已新增 `WaterDropWaveView`，用贝塞尔曲线绘制水滴轮廓、白色高光和水面波浪，使用 `ValueAnimator` 驱动波浪相位；水位来自当天真实饮水总量 / 2000ML 目标。已新增 `AddWaterIconView` 自绘按钮内水滴加号图标，未使用图片/GIF 伪装控件。
- [x] 喝水真实功能接入。200ML 渐变按钮会调用 `viewModel.addWater(200)` 写入 Room，并继续通过 `ReportDataManager.reportData("click_drink_water")` 上报点击；总量文案和水位通过 `hydrateData` Flow 实时刷新，Done 按钮关闭 Activity。旧 Compose `HydrateScreen.kt` 已删除，避免两套实现并存。
- [x] 喝水页新版验证。已执行 `xmllint` 校验资源，`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过；已安装到设备 `6aa8820f`，通过首页入口进入后 `topResumedActivity` 为 `com.example.lcb.app.HydrateActivity`，截图 `/tmp/lcb_hydrate_activity_wave.png` 和 `/tmp/lcb_hydrate_activity_after_drink.png` 确认水滴波浪和 200ML 写入刷新正常。
- [x] 喝水水滴顶部圆角细化。根据实机截图反馈，`WaterDropWaveView` 顶部轮廓已从中心单点贝塞尔改为三段带宽度的小圆弧，避免顶部继续呈现尖角；同时移除水面上方额外覆盖层，只保留水滴外壳磨砂渐变和双层波浪。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin` 通过。
- [x] 喝水水滴顶部曲线连续性修正。针对顶部圆角与两侧主体曲线交界处出现局部凸起的问题，已将顶部三段拼接弧改为单段连续 cubic 贝塞尔弧，避免中间拼接点导致切线/曲率突变；左右侧主体曲线直接从同一连续弧过渡。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin` 通过。
- [x] 喝水页右上角按钮改为 Toolbar menu action。已新增 `res/menu/menu_hydrate_toolbar.xml`，将右上角报表和设置两个按钮定义为 `app:showAsAction="always"` 的 Toolbar action；`HydrateActivity.onCreateOptionsMenu` 改为 `menuInflater.inflate(...)`，`onOptionsItemSelected` 统一处理返回、报表 action 和设置 action，其中设置 action 启动独立 `SettingsActivity`。已执行 `xmllint` 和 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 喝水页设置 action 图标内圆居中。已将 `ic_hydrate_settings.xml` 的内圆从 arc 端点路径改为明确以 `12,12` 为圆心的 cubic 圆路径，保留 Toolbar menu action 机制不变，避免中间圆视觉偏移。已执行 `xmllint --noout app/src/main/res/drawable/ic_hydrate_settings.xml` 和 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin` 通过。
- [x] 首页喝水有数据状态卡片。已用 Figma 节点 `2181:1066` 实现 Health Tools 中的 Log water 有数据卡：今日饮水总量来自 Room `hydrateData` 聚合，目标为 2000ML，圆形水量进度和 `+200ML` 按钮会实时刷新；顶部箭头仍进入独立 `HydrateActivity`，按钮将 200ML amount 传回 App 层统一调用 `viewModel.addWater(amount)` 并上报 `click_drink_water`。底部 7 天状态复用首页 Report 抽出的 `DailyStatusIcon`，0 数据显示 X，达标显示勾，未达标显示进度环，短星期文案已补齐多语言资源。已执行全部 `strings.xml` 的 `xmllint` 校验、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过；已安装到设备 `6aa8820f`，显式启动 `MainActivity` 后截图 `/tmp/lcb_home_water_card_scrolled.png` 确认卡片完整展示，并点击 `+200ML` 截图 `/tmp/lcb_home_water_card_after_add.png` 确认真实写入刷新。
- [x] 设置页 Toolbar title 居中修正。已去掉错误添加的自定义标题 `TextView`，设置页继续使用 Android 原生 `Toolbar` 自带 title API：`setSupportActionBar`、`setDisplayHomeAsUpEnabled(true)` 和 `supportActionBar?.title`。状态栏适配从给 Toolbar 加 `paddingTop` 改为设置 Toolbar 的 `topMargin`，让 Toolbar 自身保持 44dp 内容区，title 和系统返回按钮在同一内容区内上下居中。已执行 `xmllint --noout app/src/main/res/layout/activity_settings.xml app/src/main/res/values/themes.xml`、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 首页喝水卡星期文案统一。已将喝水卡底部状态控件下方的单字母星期改为与上方计步 Report 一样的三字母/本地化 `weekday_*` 文案；新增 `HomeWeekdayLabels.kt` 作为同包共享函数，计步卡和喝水卡都调用同一个 `weekdayLabel`，并删除上一版不再使用的 `weekday_short_*` 资源，避免两套星期显示逻辑分叉。已执行全部 `strings.xml` 的 `xmllint` 校验、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 首页计步卡暂停/恢复与更多图标恢复。已重新读取 Figma 首页节点 `2150:281`，确认计步卡右上角为 18dp Pause 和 More，间距 20dp；已在 `HomeStepCard` 中恢复两个 action 入口，并新增矢量资源 `ic_home_step_pause.xml`、`ic_home_step_resume.xml`、`ic_home_step_more.xml`。暂停按钮使用 `rememberSaveable` 保存界面状态，点击后在暂停和恢复图标之间切换；更多按钮先恢复视觉入口，暂不接业务逻辑。已执行 `xmllint --noout` 校验新增图标、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 首页计步卡 action 按钮 Figma 精修。已再次读取 Figma 首页节点 `2150:281`，确认右上角 Pause/More 是 18dp 图标、两个图标视觉间距 20dp；修正 `StepActionIcon`，去掉上一版 32dp 外层点击盒参与布局导致的视觉间距偏大问题，让 18dp 图标本身参与 Row 排版；Pause/More 矢量 path 已替换为 Figma SVG 导出的真实 union path，圆环、暂停条和三个点的位置/粗细与设计稿一致。已执行 `xmllint --noout app/src/main/res/drawable/ic_home_step_pause.xml app/src/main/res/drawable/ic_home_step_more.xml app/src/main/res/drawable/ic_home_step_resume.xml`、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 业务计步暂停/恢复算法封装。已新增 `StepCountingAlgorithm` 作为纯业务算法层，底层仍接 Android `TYPE_STEP_COUNTER` / `TYPE_STEP_DETECTOR` 系统 API；DataStore 中 `today_steps` 继续作为当天业务步数，新增 `step_counter_anchor_total`、`step_counting_paused`、`step_counter_resume_anchor_pending` 维护系统累计值锚点、业务暂停状态和恢复后重新锚定状态。正常计步时用系统 total 与本地锚点差值累加到业务步数；暂停时只移动锚点不增加业务步数；恢复后下一次 counter sample 先重新锚定，避免把暂停期间的系统步数补进业务步数；跨天 rollover 会把前一天业务步数写入 Room，并将新一天业务步数重置为 0。首页暂停/恢复按钮已改为读取 `HomeData.isStepCountingPaused`，点击后通过 `LcbAppViewModel.setStepCountingPaused` 写入持久化业务状态，离开页面或重启后状态仍由数据层决定。已新增 `StepCountingAlgorithmTest` 覆盖首次锚定、正常增量、暂停不增、恢复重新锚定和跨天重置；已串行执行 `./gradlew --stop && ./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:testLocalDebugUnitTest`、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:assembleLocalDebug` 通过。
- [x] 首页更多按钮步数修正弹框。已使用 Figma 节点 `2314:842` 实现居中 `Edit Steps` 弹框，按需求去掉 Time 行，只保留日期和步数；日期默认今天，可通过原生 `DatePickerDialog` 选择今天及之前日期，步数输入限制为数字并最多 7 位。保存今天会写入业务层当天步数状态，保存历史日期会 upsert 到 Room `daily_steps`，并保留已有日期 goal；新增 `HydrationRecordDao` 无关，步数 DAO 增加按日期查询。后续根据视觉反馈已优化弹框对比度：加入自定义灰色遮罩、提亮弹框卡片背景、增加阴影和描边，输入行也改为更亮的深灰，避免和首页黑色背景混淆。已补齐多语言文案和 `ic_home_dropdown.xml`，并执行 `xmllint`、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 首页步数修正弹框主题色调整。根据反馈已移除上一版自定义灰色遮罩，不再额外覆盖页面背景，只保留系统 Dialog 默认 dim；弹框本体改为更明确的深紫灰 `#2B2A33`，输入行改为 `#373642`，并保留高对比描边和阴影，避免与首页黑色背景混淆，同时不引入额外遮罩层。已执行 `xmllint`、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 首页体重卡片双状态与体重拨盘弹框。已重新读取 Figma `2150:281` 和 `2267:756`，按 Room 中是否存在体重记录切换无数据/有数据两套 UI：无数据保留 `Log weight`、`79.87%...`、`Set goal`；有数据显示 `Weight`、环形状态、当前 Kg 和 `Update`。新增 `weight_records` Room 表、`WeightData` Flow、ViewModel 写入方法和无损 1->2 迁移；点击体重卡片或按钮弹出 Figma `2318:900/2318:929` 风格 bottom sheet，拨盘保存时统一转成 Kg 的一位小数整数值并在 IO 线程持久化。已补齐相关多语言字符串，执行 `xmllint`、`./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过；已安装 local debug APK 并启动首页，`ResumedActivity` 为 `com.leafmotivation.quizguessoncolor/com.example.lcb.app.MainActivity`。
- [x] 体重拨盘改为自定义 Compose 实现。已移除 Android 系统 `NumberPicker` 和相关反射样式逻辑，避免不同 ROM 保留选中横线；`WeightNumberPicker` 改为 `LazyColumn + rememberSnapFlingBehavior` 的自定义滚轮，三行可见、中心吸附、无横线，选中项 32sp 白色加粗，上下项 24sp 灰色渐隐并缩放；滚动中心值变化时触发 `HapticFeedbackConstants.CLOCK_TICK` 轻量震动反馈。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 体重拨盘滚动动效优化。已将 `WeightNumberPicker` 的选中态从基于 `centeredIndex` 的离散切换改为基于 item 到视窗中心像素距离的连续插值，滚动过程中数字的 scale、alpha、颜色会随距离平滑变化，避免中间选项突然放大/缩小；外部值同步从 `scrollToItem` 改为 `animateScrollToItem`，降低非手势状态更新时的跳变感。动效只使用 transform/alpha/color，保持移动端渲染轻量。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 体重单位切换修正。确认 `Lbs` 是 pounds/磅，`Kg` 是 kilograms/千克，二者应按 `1kg = 2.20462262185lbs` 换算；例如 `75.0kg` 切到 Lbs 应显示约 `165.3lbs`。已修复单位切换时自定义滚轮旧位置回调覆盖换算值的问题：单位变化时通过 `key(unit)` 重建整数/小数滚轮状态，程序化同步滚轮位置时使用 `syncingToExternalValue` 抑制用户滚动回调，避免 Kg/Lbs 切换后数值不刷新或被旧值写回。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] Lbs 模式整数/小数滚轮联动修复。确认联动原因是编辑态只保存 Kg，Lbs 整数轮变化后立即执行 `Lbs -> Kg` 四舍五入，再 `Kg -> Lbs` 回算，导致小数轮被回算值带动。已将弹框内部编辑态改为保存“当前单位下的拨盘值” `pickerWeightTenths`，滑动整数轮只更新当前单位整数位并保留小数位，滑动小数轮只更新小数位；仅在单位切换和点击 Save 时进行 Kg/Lbs 换算，Room 仍统一保存 Kg。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug` 通过。
- [x] 喝水目标设置弹窗。已使用 Figma 工具读取节点 `2314:881`，按 315dp 深色圆角弹窗、Skip、Drinking goal、推荐文案、1500/2000/2500 三个选项和渐变 Next 按钮实现 `dialog_water_goal.xml`；喝水页 Toolbar 右上设置 action 改为弹出该弹窗，不再跳转设置页。新增 `water_goal_ml` DataStore 持久化字段和 `HydrateData.waterGoalMl`，保存后喝水页水滴/总量文案与首页喝水卡进度、7 天状态共用同一目标值。已补齐 8 套语言资源，执行 `xmllint`、`:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] MainActivity 三 Fragment 底部导航重构。已使用 Figma 工具读取首页 `2150:281`、数据页 `2275:3359`、我的页 `2314:594`，确认底部导航为 Home / Trends / Me 三项、Activity 级深色 tab 背景 `#18181A`。`MainActivity` 已改为 `activity_main.xml` + `FragmentContainerView` + Activity 级 Compose 底部栏，承载 `HomeFragment`、`TrendsFragment`、`MeFragment` 三个 Fragment；Home/Trends 复用现有 Compose 页面并隐藏内部底栏，Me 页新增 Figma 风格个人中心卡片、成就、每日步数/喝水/体重目标、隐私政策和版本。已补齐 `nav_me` 和 Me 页 8 套语言资源，保留 Activity 权限申请、计步传感器启动、喝水插屏入口和 tab 点击埋点；执行 `xmllint`、`:app:testLocalDebugUnitTest`、`:app:assembleLocalDebug` 通过。
- [x] Main 底部 Trends 居中修正。已将 Activity 级 `AppBottomBar` 的 Home / Trends / Me 三项从固定 84dp + `SpaceEvenly` 改为三等分 `weight(1f)` 布局，确保每个 tab 的图标和文字都在自身 1/3 区域内水平居中；同时修正 `DataGlyph` 三根柱子的绘制坐标，从偏右的 `0.32/0.58/0.82` 调整为围绕中心对称的 `0.28/0.50/0.72`。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] Health Connect 接入 Trends 真实日内曲线。已新增 `androidx.health.connect:connect-client` 依赖、Health Connect 读取权限、权限说明 Activity 与 Android 14 permission usage alias；新增 `HealthConnectRepository`，按小时聚合 `StepsRecord`、`DistanceRecord`、`TotalCaloriesBurnedRecord`、`ExerciseSessionRecord`，并通过 `HealthTrendData` 与 UI 解耦。`TrendsFragment` 使用 Health Connect 权限 contract 请求授权，授权后自动刷新；`ReportScreen` 的 Day tab 已改为 Figma 风格 4 张卡片，右侧自绘 00:00-23:59 柱状曲线，未授权/不可用/读取失败时显示本地总量和授权提示。已补齐 8 套语言资源，执行 `xmllint`、`:app:compileLocalDebugKotlin`、`:app:testLocalDebugUnitTest`、`:app:assembleLocalDebug` 通过。
- [x] Trends Health Connect 空数据回退修正。确认首页步数来自 App 本地传感器业务计步，Health Connect 不会自动包含该本地数据；当 Health Connect 已授权但当天没有写入步数时，原 Day tab 会完全使用 Health Connect 的 0 值。已将 Trends Day 总量合并逻辑改为：Health Connect 指标有值时优先使用真实健康数据，无值时回退到首页本地步数推导出的步数/热量/距离/运动时间；小时曲线仍仅在 Health Connect 返回真实 bucket 数据时显示，避免伪造时间分布。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] App 活跃时计步实时性修正。确认杀掉 App 后步数会更新，是因为 `TYPE_STEP_COUNTER` 下次启动可读取系统累计值；App 活跃时仅依赖 counter 会受系统事件分发/批处理影响，不一定实时刷新。已将 `StepSensorRepository` 改为同时注册 `TYPE_STEP_COUNTER` 和 `TYPE_STEP_DETECTOR`：detector 事件用于前台即时加步，counter 继续作为累计值校准和进程重启后的补步来源；业务算法新增 `countedDelta` 和 `stepsAlreadyCounted`，counter 上报时会扣除 detector 已经计入的步数，降低重复计数风险。已补充 `StepCountingAlgorithmTest` 覆盖 detector 增量和 counter 去重，并执行 `:app:compileLocalDebugKotlin`、`:app:testLocalDebugUnitTest` 通过。
- [x] App 前台系统计步同步补强。根据实机现象确认“杀掉 App 后摇晃手机，重进 App 能看到最新步数”，说明系统 `TYPE_STEP_COUNTER` 累计值正常更新，问题在部分 ROM 对已注册前台 listener 不持续推送最新 on-change 事件。已在 `StepSensorRepository` 中保留系统 `TYPE_STEP_COUNTER` / `TYPE_STEP_DETECTOR` 主链路，不增加加速度模拟计步；新增前台 2 秒轻量重新注册 counter listener 的同步机制，让 SensorManager 返回最新系统累计快照，再继续走现有业务计步算法、暂停/恢复和 detector 去重逻辑。已执行 `:app:compileLocalDebugKotlin`、`:app:testLocalDebugUnitTest`、`:app:assembleLocalDebug` 通过。
- [x] 计步监听前后台生命周期控制。已将 `MainActivity` 的计步监听从 `onCreate` 移到 `onStart` 启动，并在 `onStop` 调用 `LcbAppViewModel.deactivateStepSensor()` 停止系统传感器监听和 2 秒 counter 刷新任务；进入后台、打开喝水页/设置页时不再保留前台刷新注册，返回首页后重新注册并用系统 counter 累计值补齐期间步数。已串行执行 `./gradlew --stop && ./gradlew --no-daemon ... :app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过；已安装实机验证，前台 `sensorservice` 可见 `StepSensorRepository` active，按 Home 后当前连接中不再出现 app 的 `StepSensorRepository`。
- [x] Trends Step 本地小时曲线修复。确认问题是 Trends Day 页总步数已经回退到本地业务计步，但小时曲线仍整页复用 Health Connect bucket；当 Health Connect 有 Calorie bucket 但 Step bucket 为 0 时，Step 数字有值而曲线为空。已新增 Room 表 `hourly_steps`（数据库 v3，`date + hour` 主键）按小时持久化本地计步增量，`AppPreferences.recordStepCounter/recordDetectedStep/setTodaySteps` 会在 IO 线程写入小时增量；`ReportData` 增加 `trendBuckets`，Day 页按指标分别 fallback，Step/Distance/Time 无 Health 小时数据时使用本地小时数据，Calorie 有 Health 小时数据时继续使用 Health。对升级前已存在的当天总步数但无小时记录的情况，先把未知总量归入当前小时显示一根本地柱，后续新增步数按真实小时累计。已执行 `:app:compileLocalDebugKotlin :app:testLocalDebugUnitTest :app:assembleLocalDebug` 通过；已安装实机截图 `/tmp/lcb_trends_step_curve_fixed_2.png`，确认 Step 曲线出现本地绿色柱。
- [x] Trends 页 Figma 层级还原。已重新读取 Figma 节点 `2275:3359` 的截图、metadata 和 design context，确认页面结构为顶部独立胶囊 Tab、下方日期/周期左右切换、再下方独立指标卡片列表；已移除原来包住 Tab/日期/所有图表的大卡片，改为 32dp 胶囊按钮（选中白底黑字，未选中 `#232227`）、12dp 左右箭头周期切换和 343dp 宽独立指标卡片。Day/Week/Month 共用周期切换逻辑，Day 页按选中日期刷新 Health Connect 并保留本地数据回退；Week/Month 使用选中周期统计 Room 历史数据。已执行 `:app:compileLocalDebugKotlin` 通过；首次增量 `:app:assembleLocalDebug` 因 Gradle 中间产物 `R.jar` 缺失失败，随后执行 `:app:clean :app:assembleLocalDebug` 干净构建通过。
- [x] Trends Tab 与时间切换稳定性优化。根据反馈将顶部 Day/Week/Month Tab 从固定 80dp 左排列改为容器内三等分 `weight(1f)`，保持居中和平分宽度；日期/周期切换时不再为等待 Health Connect 新日期数据注入 `Checking` 状态，避免 banner 或卡片树被整体替换，改为先用本地/历史数据定向更新卡片数值和曲线，Health Connect 结果返回后再更新同一组卡片内容。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 通用数值更新动画扩展。已新增 `String.AnimatedValueText` Compose 扩展方法，内部使用 `AnimatedContent` 做 180ms 以内的轻量纵向滑入/淡入、滑出/淡出，并读取系统 `ANIMATOR_DURATION_SCALE`，当系统关闭动画时自动退化为普通 `Text`；Trends 指标卡片的 Step、Calorie、Distance、Time 数值已改为复用该扩展，后续首页、喝水、体重等数值更新可直接复用同一方法。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 移除 Health Connect 依赖并改回本地趋势数据。根据兼容性反馈，已完全移除 Health Connect：删除 `androidx.health.connect:connect-client` 依赖、健康权限声明、权限说明 Activity/alias、`HealthConnectRepository`、ViewModel 的 Health Connect 状态流和 TrendsFragment 的权限请求逻辑；`ReportScreen` Day 页改为只使用本地业务计步数据和 Room `hourly_steps` 小时步数，Step/Calorie/Distance/Time 曲线都从同一组本地小时步数推导，Calorie 不再被 Health Connect 的总热量 bucket 抢占导致曲线变平。已删除各语言 Health Connect 文案并执行全部 strings XML 校验；已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] Trends Week/Month 改为与 Day 同款 UI。已清理 `ReportScreen` 中旧的 Week/Month 大图表和统计块，统一改为 Step、Calorie、Distance、Time 四张指标卡；Day 使用 24 小时本地桶，Week 使用 7 天本地历史，Month 使用当月日期范围，本质差异只保留时间范围和横轴起止标签。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 首页与喝水页数值更新动画。已将 Compose 数值动画扩展增强为同时支持 `String` 和 `AnnotatedString`，首页大步数、Goal、Daily avg、运动三指标、喝水总量、喝水百分比和体重数值都切换为 `AnimatedValueText`；喝水独立 Activity 是原生 XML，因此新增 `TextView.setAnimatedValueText` 扩展，保留 `SpannableString` 的颜色和字号样式，同时为 `0/2000ML` 这类总量/目标文案增加同类滑入淡入动画。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 首页进度动画。已新增 `HomeProgressAnimation.kt`，统一封装 420ms `FastOutSlowInEasing` 的进度动画并读取系统 `ANIMATOR_DURATION_SCALE`，系统关闭动画时直接显示目标值；首页步数主进度条、每日状态圆环、喝水圆环和体重圆环都改为复用该动画。`weekdayLabel` 工具函数已合并到 `HomeDailyStatus.kt`，减少跨文件小工具漂移。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 首页 water/weight 总进度首次动画补齐。确认上一版 `animateFloatAsState` 在首次组合时会直接以目标值作为初始值，因此 water/weight 圆环进入首页时看不到从 0 到当前总进度的动画；已将 `HomeProgressAnimation.kt` 改为 `Animatable(0f)` + `LaunchedEffect`，首次展示从 0 平滑跑到目标进度，后续数据变化继续从当前值过渡，系统关闭动画时仍直接显示目标值。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 数据报表详情页。已使用 Figma 工具读取节点 `2325:960`，按深色 Report 详情页实现独立 `ReportDetailActivity` 和 Compose `ReportDetailScreen`：顶部返回/标题、Day/Week/Month 时间粒度、Step/Calorie/Distance/Time 指标切换、时间范围左右切换、Avg/Total 汇总卡、按真实本地数据绘制的柱状图/选中提示、Statistics 双卡片。Trends 页面四张指标卡已增加点击入口，点击后带入对应初始指标进入详情页；数据全部来自现有 `ReportData`、小时步数桶和 Room 日历史，不使用 mock。已执行 `xmllint --noout app/src/main/AndroidManifest.xml app/src/main/res/values/strings.xml`、`:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 喝水设置弹框与喝水报表入口优化。已将喝水目标 Dialog 宽度改为 show 前设置到内容根 View，Window 只负责透明背景、dim 和居中，避免 show 后二次 `setLayout` 导致先偏右再居中的跳动；Dialog 内部选项使用 match/weight 宽度适配屏幕。Toolbar 报表 action 已进入新增 `HydrateReportActivity`，报表页按真实喝水记录聚合 Day/Week/Month 水量曲线、Avg/Total 和统计卡；因为喝水只有单一指标，已移除 Water 单项 tab/chip。已执行全部相关 `strings.xml`、`dialog_water_goal.xml`、`AndroidManifest.xml` 的 `xmllint` 校验，并执行 `:app:compileLocalDebugKotlin` 通过；`:app:assembleLocalDebug` 曾被中断，未作为本轮最终验证结果。
- [x] 成就页与真实成就规则。已使用 Figma 工具读取成就解锁节点 `2330:2359` 和未解锁节点 `2330:2263`，导出页面 3x 参考图、背景图和 12 套奖章原始图片；运行时奖章资源统一压到 512px，保持 Figma 原图来源同时降低 Compose 同屏解码内存。新增 `AchievementActivity` 和 `AchievementScreen`，按 Figma 黑色背景、顶部氛围光、返回按钮、标题/副标题和 3 列 4 行奖章布局实现。成就业务规则统一封装在 `AchievementModels.kt`：First Steps=累计 1,000 步，Goal Getter=首次达成每日步数目标，10K Day=单日 10,000 步，100K Club=累计 100,000 步，On Fire/Consistent/Unstoppable=连续 3/7/30 天步数达标，Hydrated=首次达成每日饮水目标，Aqua Streak=连续 7 天饮水达标，Goal Weight=达到当前体重模块默认目标 74.0kg，Steady=连续 30 天记录体重，Explorer=累计步行 10km。首页 Achievements 卡、Me 页 Achievements 卡、设置页 Achievements 行均已接入跳转，首页/Me 页显示真实已解锁数量。已执行 `xmllint`、`:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 设置页 Achievements 入口只展示已解锁成就。已让 `SettingsActivity` 同时订阅 `homeData`、`hydrateData`、`weightData`，复用 `AchievementModels.kt` 的真实成就计算结果，并只将 `unlocked=true` 的成就传给 `SettingsScreen`。设置页 Achievements 行右侧现在只显示最多 3 个已解锁奖章原图预览，超过部分显示 `+N`；没有解锁时显示 `0 unlocked`，不展示未解锁奖章或总数。已执行 `xmllint`、`:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] Me 页 Achievements 卡片移除固定占位图标。已将 `MeScreen` 中原来固定 `repeat(6)` 绘制的奖章预览改为复用 `AchievementModels.kt` 的真实已解锁列表，只展示 `unlocked=true` 的奖章原图，超过 6 个才显示 `+N`；没有解锁时显示 `0 unlocked`，顶部 `3/12` 与下方图标数量保持一致。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 删除独立 SettingsActivity 并迁移设置能力到 Me 页。已删除 `SettingsActivity.kt`、`activity_settings.xml`、旧 `SettingsScreen.kt` 和 Manifest 注册；语言切换抽成 `LanguageSelectionSheet` 共享组件并移动到 Me 页 item，Feedback 也移动到 Me 页 item，二者均使用 Me 页现有列表风格和自绘同风格 icon。首页设置按钮现在切到 Me tab，不再启动独立设置页；Privacy Policy 继续在 Me 页打开浏览器。已移除 ProGuard 中已删除 Activity 的 keep 规则，并确认 `SettingsActivity/SettingsScreen/activity_settings` 无代码残留引用；已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] Me 页语言选择弹框改为独立 BottomSheet。已将原来自绘全屏 `Box` sheet 改为 Material3 `ModalBottomSheet`，作为独立弹层显示，避免被 Activity 级底部导航栏遮挡；弹框整体改为内容包裹高度，收紧顶部手柄、标题、语言行、按钮和底部间距，语言列表使用 343dp 内宽小卡片、38dp 行高和 44dp 操作按钮，视觉更小巧。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] Me 页目标设置入口改为业务弹框。已将 Me 页 `Daily step goal` 从跳转首页改为直接复用首页 `StepGoalBottomSheet` 并保存到 `setStepGoal`；`Daily water goal` 从跳转喝水页改为新增 Compose 版 `WaterGoalDialog`，复用 Hydrate 业务弹框的 1500/2000/2500ML 目标选择并保存到 `setWaterGoalMl`；`Weight goal` 从跳转首页改为直接复用 `WeightEntryBottomSheet` 并保存到 `setWeightForDate`。MeFragment 不再为这三个入口调用页面跳转。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] 业务界面文案国际化复扫与补齐。已扫描非广告业务 Kotlin/XML 文案，保留 `ML`、`Min`、`Kcal`、`KM`、`Kg`、`Lbs`、`Pro`、`10K/100K` 等单位和专有词不翻译；将首页 `Daily avg` 硬编码改为 `home_daily_avg_value` 资源，将 Feedback 邮件标题改为 `feedback_email_subject` 资源，并删除未使用的旧 `activity_main_home.xml` 中 `Hello World!` 模板布局。已补齐 `home_goal_value`、`home_daily_avg_value`、Health Tools、Achievements、Report detail 等 39 个缺失 key 到 de/es/fr/pt/ja/ko/zh-rCN；默认资源除 `app_name translatable=false` 外 140 个可翻译 key 在 7 套语言中均 0 缺失。广告相关按后续要求暂不纳入本轮整理范围。已执行 strings XML `xmllint`、`:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] Me 页目标弹框、语言弹框和报表详情底部文案复查。已确认体重弹框、喝水目标弹框、步数目标弹框均使用现有业务资源，单位类 `ML/Kcal/Km/Min/Kg/Lbs` 保持不翻译；语言弹框改为固定自名展示 `English/Deutsch/Español/Français/Português/日本語/한국어/简体中文`，Me 页当前语言值同步使用同一自名，符合常规语言选择器习惯。报表详情底部原硬编码 `Statistics`、`Most Active Time/Day`、`Most Relaxing Time/Day`、`Steps` 已抽到 `report_detail_*` 资源并补齐 8 套语言；`xmllint` 校验全部 strings 通过，默认资源与 de/es/fr/pt/ja/ko/zh-rCN 可翻译 key 均 0 缺失。已执行 `./gradlew --no-daemon -Dorg.gradle.jvmargs='-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8' :app:compileLocalDebugKotlin :app:assembleLocalDebug`，构建通过。
- [x] 底部导航 Me 文案修复。已确认 Activity 级底部导航 `Me` 不是业务硬编码，而是日语资源 `nav_me` 漏翻以及 Activity 底栏只显式传入 `Trends` 文案导致排查不直观；已将日语 `nav_me` 从 `Me` 改为 `マイ`，并扩展 `AppBottomBar` 支持 `homeLabel/dataLabel/meLabel`，`MainActivity` 三个底栏文案均从当前 `localizedContext` 明确取值。扫描确认除默认英文资源外不再存在未本地化的 `Me`；已执行 `xmllint`、资源 key 缺失对比、`:app:compileLocalDebugKotlin` 和 `:app:assembleLocalDebug` 通过。
- [x] 目标与语言弹框改为 BottomSheetDialogFragment。根据反馈放弃继续在 Compose 弹层里补字符串传参，新增 `LocalizedBottomSheetDialogFragment` 基类和 `LanguageBottomSheetFragment`、`StepGoalBottomSheetFragment`、`WaterGoalBottomSheetFragment`、`WeightEntryBottomSheetFragment` 四个独立 Fragment；每个弹框直接读取 `LcbAppViewModel.language.value`，用 `localizedContext` 创建 UI 和读取 `settings_select_language/settings_cancel/settings_confirm/home_set_step_goal/home_step_goal_label/hydrate_goal_* /home_weight_*` 等资源。Me 页四个入口、首页步数目标/体重入口、喝水页右上角设置入口均已切到 Fragment 版；旧 Compose 版本 `LanguageSelectionSheet`、`StepGoalBottomSheet`、`WeightEntryBottomSheet`、`WaterGoalDialog` 已删除，只保留语言自名工具 `LanguageLabels.kt`。已执行 strings XML 校验、可翻译 key 缺失对比、`:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug`，全部通过。
- [x] 首页更多按钮编辑步数弹框改为 DialogFragment。已新增居中的 `StepCorrectionDialogFragment`，从 `LcbAppViewModel.homeData` 读取当天/历史步数并通过 `setStepsForDate` 保存，日期选择继续限制到今天及以前；弹框标题、说明、日期、步数、取消、保存、Today 日期格式等文案均通过当前 `language.value` 的 `localizedContext` 读取资源。首页计步卡更多按钮已改为由 `HomeFragment` 显示该 DialogFragment，旧 Compose `HomeStepCorrectionDialog.kt` 已删除，避免国际化上下文回落到默认英文。已执行 `xmllint`、资源 key 缺失对比、`:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug`，全部通过。
- [x] `lcb_res` 图片文件名前缀调整。已将 `lcb_res` 下所有未加前缀的图片文件统一重命名为 `3_*`，包含普通 PNG 和 `.9.png` 图片；已反向检查确认当前不存在未加 `3_` 前缀的 `png/jpg/jpeg/webp/gif` 图片。`config.json` 中的 `novi_` 内容是文案 key，不是图片文件路径引用，本轮未修改配置文案。
- [x] 体重弹框顶部细节优化。已在 `WeightEntryBottomSheetFragment` 顶部增加小巧圆角拖拽条，并将弹框顶部圆角从 12dp 调整到 18dp；标题与顶部边缘通过拖拽条 top/bottom margin 拉开距离，避免标题贴近边框。通用 `handle` 方法增加宽高参数，保持其它弹框默认拖拽条尺寸不变。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] 体重滚轮滑动后数字不可见修复。确认原因是 `NumberPicker` 滚动时会使用内部 selector wheel paint 绘制数字，之前只给子 `TextView/EditText` 设置了白色，滚动绘制仍可能回到默认深色，导致深色弹框上看不见。已将滚轮样式拆为 `tintNumberPicker` 配置和 `applyNumberPickerTextStyle` 应用两层，统一设置内部 wheel paint 与子 TextView 的白色粗体样式，并在滚动停止后重新应用。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] 体重滚轮改为自定义 View。由于系统 `NumberPicker` 在滚动期间仍存在内部绘制颜色不可控、滑动后数字消失以及震动反馈不稳定的问题，已移除体重弹框中的 `NumberPicker` 依赖，新增 `WeightWheelPickerView` 自绘滚轮：统一绘制中心选中数字、上下候选数字和选中分割线，松手后平滑吸附到最近数字，并在选中值变化时触发 `HapticFeedbackConstants.CLOCK_TICK` 震动反馈。体重弹框保留原有 Lbs/Kg 切换、整数/小数两个滚轮和保存逻辑。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] 体重滚轮横线移除与滚动动效增强。已移除 `WeightWheelPickerView` 中心选中区域的上下横线绘制；滚动时数字不再用选中/未选中阈值硬切换，而是根据距离中心的进度连续插值字号、透明度和字体权重，并对纵向位移做轻量 easing，让数字靠近中心时逐渐放大变亮、离开中心时平滑淡出。保留松手吸附和 `CLOCK_TICK` 震动反馈。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] Launcher 桌面步数组件。已使用 Figma 工具读取节点 `2354:222`，按 343x271 玻璃卡实现原生 `StepLauncherWidgetView`：顶部显示实时 `HH:mm` 和日期，右上角绘制 GoStep 绿色 logo，中部显示周日到周六的真实步数达标状态，底部显示真实步数推导的 Min/Kcal/Km。新增 `StepLauncherWidgetManager` 在 `LcbApp.onCreate()` 中安装 provider，复用模板 launcher 混淆后的 API：`maxquicklitememory` 注册 provider / grid ready / canPlace / isRendered，`convertsafepower` 监听渲染完成，`localproshield()` 刷新桌面；默认放置在 page=0、left=0、top=0、spanX=4、spanY=3，桌面 ready 后会先预检查位置。数据来自 `AppPreferences.homeData`，View 实例由 Application context 创建并复用，不持有 Activity；点击组件打开 `MainActivity`。已执行 `:app:compileLocalDebugKotlin`、`:app:assembleLocalDebug` 通过。
- [x] `metaautovault()` 补齐 app 模块 Activity。已扫描 `app/src/main/AndroidManifest.xml` 和 app 模块 Kotlin 类，确认当前 app 自己声明的 Activity 为 `MainActivity`、`HydrateActivity`、`ReportDetailActivity`、`HydrateReportActivity`、`AchievementActivity`；已将这 5 个全部返回到 `LcbApp.metaautovault()`，不包含库依赖中的 Activity。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] Launcher 组件不显示修复。已对照 `HomeCustomViewMinimalDemo.kt` 的接入方式检查，确认此前 `StepLauncherWidgetManager` 对主组件额外使用 `canPlaceHomeCustomView` 结果做 provider 提交门控；当默认位置被桌面判定不可放置时，provider 会提交 0 项，表现为组件完全不显示。已改为与 demo 一致的单一 `ViewSpec` 表：注册 provider 后每次刷新直接 emit 当前组件，grid ready 和 install 完成后都调用 `localproshield()` 主动刷新，不再用预检查阻断主组件显示；View 仍由 Application context 创建并复用，不持有 Activity。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] Launcher 组件坐标重叠修复。根据运行日志确认 provider 已返回 `gostep_launcher_step_widget`，但固定坐标 `page=0 left=0 top=0 spanX=4 spanY=3` 与模板已有 `Time View` 重叠，被桌面合并阶段跳过。已在 `StepLauncherWidgetManager` 增加 grid ready 后的自动放置搜索：优先同页向下寻找可容纳 4x3 的位置，再尝试后续页面；找到后更新 `ViewSpec` 坐标并调用 `localproshield()` 刷新。若没有可用位置，会保留默认坐标并打印日志，便于继续查看冲突原因。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] Launcher 刷新链路 NPE 收敛。根据新日志确认异常发生在 `isHomeCustomViewRendered -> refreshHomeScreen -> fetchGridItems()` 路径，业务数据流每次更新时查询 rendered 并刷新，和模板桌面刷新生命周期耦合过重。已按 `HomeCustomViewMinimalDemo.kt` 的最小链路收敛：数据更新只更新并 `invalidate()` 复用的 `StepLauncherWidgetView`，不再调用 rendered 查询和刷新；只在 install、grid ready、跨天重置时通过 `requestRefresh()` 主线程合并触发 `localproshield()`。同时将默认坐标改为 `page=0 left=0 top=1 spanX=4 spanY=3`，避开模板顶部 `Time View` 占用的第 0 行，去掉 grid ready 内的自动搜索，降低对库内部反射状态的依赖。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] Launcher 组件二次返回桌面消失修复。根据实机日志确认 4x3 组件会因桌面已有 173 个 DB 项被放到第 7 页，后续回 APP 再返回桌面时，grid ready 再次执行位置搜索，可能把已渲染的动态组件自身也纳入占用判断并重新移动。已将组件高度从 `spanY=3` 调整为 `spanY=2`，更容易落在首页 `page=0 top=1`，并新增 `placementLocked`：组件渲染成功后锁定当前坐标，后续 grid ready 只刷新 provider，不再重新搜索和移动。已执行 `:app:installLocalDebug` 成功安装到设备。
- [x] Launcher 组件首次出现后立刻消失修复。根据现象判断，`spanY=2` 固定位置已经能在首页出现，但随后 grid ready 再执行 `canPlace` 搜索时，可能把刚合并进去的动态组件或桌面状态变化当成冲突，导致 provider 坐标被移动到后续页面，看起来像“立刻消失”。已彻底移除 grid ready 内的自动位置搜索和 `placementLocked` 状态，只固定提交 `page=0 left=0 top=1 spanX=4 spanY=2`；grid ready 只触发一次节流刷新，不再修改坐标。已执行 `:app:installLocalDebug` 成功安装到设备。
- [x] 业务广告接入。已基于 `BusinessAdExt.kt` 的快捷方法接入广告，不处理 SDK 内部请求/展示/关闭细节。新增 app 级 `BusinessAdGate` 统一节流插屏：第一次跨功能入口不弹，之后至少间隔 60 秒才请求、展示成功后至少间隔 120 秒，并在展示成功后重置计数，避免各入口单独计数导致过频。插屏接入点：进入喝水页、进入成就页、Trends 进入步数/热量/距离/时间详情、喝水页进入喝水报表；不在喝水按钮、底部 tab、设置弹框等高频操作上弹。原生广告接入点：Trends 页原有保留，新增 Me 页底部、成就页底部、步数详情页底部、喝水报表页底部；独立 Compose Activity 改为 `FragmentActivity` 以复用现有 `NativeAdSlot`/`loadNative`。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] 原生广告首屏可见优化。根据广告合规风险调整原生广告位置，避免加载后需要滑动到底部才能看到：Trends 页广告上移到时间切换和指标卡之间；Me 页广告上移到 Pro 卡之后、成就卡之前；成就页广告上移到标题/总数之后、成就网格之前；步数详情页广告上移到范围/指标 tab 之后、SummaryCard 之前；喝水报表页广告上移到范围 tab 之后、SummaryCard 之前。保留内部自动控制隐藏/显示，不增加额外展示逻辑。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] 编辑步数日期选择多机型兼容修复。排查发现首页“更多 -> 编辑步数”弹框点击日期时使用系统 `DatePickerDialog`，它会依赖 ROM/主题里的 DatePicker 资源，在部分厂商深色主题或动态语言 Context 下存在崩溃/样式异常风险；截图中的红色栈主要是 Glide 解码广告资源失败，但日期路径本身也需要规避系统控件差异。已新增 app 内自绘 `StepDatePickerDialogFragment`，使用普通 `TextView/GridLayout` 绘制月切换和日历网格，限制最大日期为今天，并通过父 `StepCorrectionDialogFragment` 回写选中日期和对应历史步数；不再调用系统 `DatePickerDialog`，提升多机型适配稳定性。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] 喝水页水滴进度满量修复。确认喝水页传入 `WaterDropWaveView` 的数据已经是当天真实喝水总量除以每日目标量，问题在自定义 View 内部把水面高度固定映射到 `34%~92%`，导致 100% 时顶部仍然无法灌满。已新增 `waterSurfaceY()`，将真实进度映射到完整水滴高度；达到满量时水面移动到水滴顶部之外，通过水滴 path 裁剪后视觉上完整填满顶部。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] 语言选择增加跟随系统。已新增 `system` 语言偏好值和 `language_system` 多语言文案，语言弹框第一项为“跟随系统”，国家语言仍保留自名展示；Me 页当前语言值同步显示“跟随系统”。新增 `resolveAppLanguageCode()`，`localizedContext()` 和 `createLocalizedConfiguration()` 会在偏好为 `system` 时根据当前 `Configuration.locales` 解析到已支持语言，不支持则回退英文；系统语言变化依赖 Android 配置变化/Compose `LocalConfiguration` 触发刷新，没有在 `onResume` 中做额外处理。已执行 strings `xmllint` 和 `:app:compileLocalDebugKotlin` 通过。
- [x] 默认语言改为跟随系统。已确认上一版虽然支持 `system`，但 DataStore 空语言值、`AppData` 默认值和 `LcbAppViewModel.language` 初始值仍是 `en`，会导致新用户默认选中英文。已将空值/未设置语言归一化为 `system`，语言弹框默认选中 `SystemLanguageCode`；非法或不支持的显式语言值仍回退 `en`，系统语言不支持时也继续由解析层回退英文。已执行 `:app:compileLocalDebugKotlin` 通过。
- [x] 全局模型整理与 release 混淆规则加固。已扫描 app 模块 57 个 Kotlin 文件，优先拆出大 UI 文件内的纯 data/enum：新增 `ReportDetailModels.kt`、`ReportScreenModels.kt`、`HydrateReportModels.kt`、`SheetModels.kt`，将报表详情、Trends 周期数据、喝水报表和弹框语言/体重单位模型从页面文件中抽离；同时把 Me 页和语言弹框重复的语言自名列表收敛到 `LanguageLabels.kt` 共享，减少后续漏改。保留未抽离项仅限单文件局部状态或强绑定算法上下文，例如 `StepCountingAlgorithm.State/Result`、launcher `ViewSpec`、`DailyStatusState`、`AchievementDefinition`。已完善 `app/proguard-rules.pro`：补齐所有 app Activity、Fragment/DialogFragment、`LcbAppViewModel`、XML 自定义 View、launcher 自定义 View/Manager、Intent/valueOf 使用的 enum、Room 全部实体和 DAO 生成类；第三方 SDK 仍依赖各自 consumer rules，不额外 keep SDK。验证：先执行 `:app:compileLocalDebugKotlin` 通过；首次 local release 因 Gradle transform 缓存污染清理单个缓存目录后恢复，随后使用 8GB 临时 heap 执行 `:app:assembleLocalRelease`，R8 full mode/minify 和 APK 打包通过。构建仅剩三方 SDK/R8 warning（launcher stack map、Kwai companion/missing optional class、SDK 字符串格式等），未出现 app 自有混淆错误。
